# schoolpalm/message-delivery

A lightweight PHP message delivery package providing channels, providers and a small delivery orchestration layer.

This repository contains a scaffolded package with:

- src/ : package source
- config/ : configuration
- tests/ : phpunit tests

See the examples/ folder for a custom provider example.
